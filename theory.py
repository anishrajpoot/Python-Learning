#List[] =[4,78,2,0]  it is mutable -> we can change data inside List, allow slicing
append(x) or insert(index ,x)
remove(x) or pop(index)

#tuple()= (5,2,8,1) ->it is immutable ->we can't insert directely we need to insert in lsit then comvert into tuple , allow slicing


# Set(4,8,3,90)-> it is mutable and also dont support dublicate element ,dont allow slicing
add(), update(), remove(), discard() , pop()

# Dictionary -> { key: Values} it is mutable  , dont allow slicing
can be added , removed, update 
